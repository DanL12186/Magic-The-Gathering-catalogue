<% title("MTG - #{params[:artist]}") %>

<h1 class='text-center'> <%= params[:artist] %> </h1>

<% @cards.each_with_index do | card, idx | %> 
  <h2> <%= truncate_at_three_words(card.name) %> </h2> 

  <div class = "col-sm-2 filter"> 
    <%= link_to ( image_tag card.img_url, class: "filter-img #{card.edition.downcase}", lazy: lazy_load?(idx, 5)), card_path(card.edition, ensure_not_flip_card_back(card.card_number, card.name, card.flip_card_multiverse_id)) %>
  </div>
<% end %>